# docker build -f DockerFile . -t nightrang3r/langflow:1.0.10
# docker run -d --add-host=host.docker.internal:host-gateway -p 7860:7860 -it --name langflow nightrang3r/langflow:1.0.10

FROM python:3.12.4-slim
RUN apt-get update && apt-get install -y --no-install-recommends build-essential net-tools git nano
RUN pip install --upgrade pip
RUN pip install langflow==1.0.10
RUN pip install redis
ENV LANGFLOW_HOST=0.0.0.0
ENV DO_NOT_TRACK=true
EXPOSE 7860
# Run langflow when the container launches
CMD ["langflow", "run"]
